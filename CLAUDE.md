# CLAUDE.md — foodlog-lite 開発規約

このリポジトリは知人向けの簡易版食事ログPWA（foodlogのフォーク）。設計判断はclaude.aiのプロジェクトスレッドで行い、ここには実装規約のみを置く。**このファイルにも、リポジトリにも、個人の健康データ・氏名・数値を絶対に書かない**（リポジトリは公開。データはユーザー端末のlocalStorageのみ）。

## liteの設計原則（変更禁止。変更はチャット側の決定が必要）
1. **記録するだけ。評価も採点もしない**：ゲージ・達成色分け・目標比較・改善提案をUIに出さない。例外として**PFCの概算は「事実の目安」としてのみ表示**（`showPFC`。目標値・達成判定・警告色を付けたら違反）。詳細数値＋振り返りタブの本体互換表示は `showNutrientNumbers`。データは裏で保存し続ける（第二幕の人間分析用）。
2. **罰感ゼロ**：ストリークが途切れても「また今日から」＋過去最長の併記のみ。赤色・警告表現は禁止。記録数は事実だけを述べる（「多いほど良い」のトーンを付けない）。
3. **記録という行為は必ず認める（固定給）**：AIコメントは内容にかかわらず記録行為を認める。内容褒め（ボーナス）はフェーズ2解禁後のみ・良い選択1品を理由付きで・1回の返答で最大2つ。悪い選択への言及・比較・改善提案・数値言及・健康リスク・説教は禁止。コメントは2モード＝締め（`getCheer(true)`、day.closed記録・節目祝いあり）と途中経過（`getCheer(false)`、締めの総括禁止）。
4. **フェーズ制**：初記録日を1日目として `LITE_CONFIG.phase2StartDay`（既定8）日目からフェーズ2。`phase2Manual` で手動上書き可。フェーズ1は内容への言及自体を全面禁止（プロンプトに品目リストを渡さない実装で担保）。
5. **設定の外部化**：機能の表示/非表示と文言は app.js 冒頭の `LITE_CONFIG` に集約。**全フラグtrueで本体foodlog相当の挙動**になる作りを維持し、新機能を足すときも必ずフラグで分岐する（本体P3への逆輸入を想定）。
6. **タイムラインが主役UI**：横棒5:00〜25:00・●は単色・時刻なしは「⏱なし」枠。タイムラインにも数値・評価・色分けを持ち込まない。
7. **30秒ルール**（本体から継承）：記録は1日30秒以内。入力を増やす機能は原則却下。

## アーキテクチャ
- **1種類1ファイル・最新版のみ**：app.js（全ロジック）/ index.html（構造とCSS）/ sw.js / manifest.json。ビルドなし・依存なし・フレームワークなし。ファイル分割やnpm導入を提案しない（test.js・smoke.jsは開発時のみ使用でアプリからは参照されない）。
- ホスティング：GitHub Pages。sw.jsのキャッシュ運用により、更新は「アプリを閉じて開き直す」で反映。
- データ：localStorage（`mealoglite:data`）。**保存キーは本体(mealog:)と分離**——GitHub Pagesは同一オリジンでlocalStorageを共有するため、本体と同じ端末で開いてもデータが混ざらないようにしている。この分離を壊さない。
- Gist同期：コードは本体から温存（`showGistSync: false` でUI非表示）。GIST_FILEも本体と別名（`foodlog-lite-data.json`）。
- AI機能：Anthropic API直叩き。食事概算=Haiku、写真=Sonnet、締めコメント=`LITE_CONFIG.commentModel`（既定Haiku。品質不足ならここをSonnetに）。APIキーはlocalStorageのみ。

## データモデル（day オブジェクト）
本体スキーマ互換＋ `closed`（bool、「今日はここまで」の日次締め）と foods の `f`（脂質g概算。本体・旧データには無い→null安全に扱いF表示は省略）を追加。
`{ foods: [{name, p, f, c, veg, omega3, fiber, cat, t}], closed, acts, workout, sleep, bedtime, waketime, weight, muscle, fatpct, rhr, steps, mood, wrist, creatine, vitd, comment, _m }`
- 非表示のフィールド（p/c/veg等）も**推定・保存は続ける**。後方互換を壊さない。旧フィールドはload()で移行。新フィールドはnull安全に。
- SEEDは空（知人の初期状態はまっさら）。

## 変更時の必須手順
1. `node --check app.js` で構文確認
2. `node test.js` — 純関数テスト（フェーズ切替・ストリーク・タイムライン座標変換）。純関数は app.js の `pure:begin`〜`pure:end` ブロックに置き、自己完結（外部参照なし）を保つ
3. UI変更時はjsdomスモークテスト：`npm i --no-save jsdom && node smoke.js`（スマホ390px/PC1280pxの両方。900px境界で切替）
4. localStorageの既存データで壊れないか（移行コードの確認。本体からのデータ移入もJSON復元で通ること）
5. iOS Safari確認項目（README参照）：ホーム画面追加起動／写真アップロードのEXIF時刻取得（HEIC失敗時のフォールバック）／7日不使用時のデータ削除リスク注記
6. コミットメッセージは日本語可・変更理由を1行

## してはいけないこと
- 評価・採点・数値表示をフラグなしでUIに追加する（liteの存在理由が消える）
- 保存キー（mealoglite:）や既存データスキーマの破壊的変更・キー名変更
- localStorage/sessionStorage前提を壊す（オフライン動作が必須）
- APIキー・トークン・健康データをコードやコミットに含める
- 機能追加の独断（実装はここ、機能の採否はチャット側の審査）
